# Single-folder run (links work on YOUR machine)

```bash
cd c12-ship
export ALLOW_AGENT_EXECUTION=true
./run.sh
```

Open **http://127.0.0.1:8080/** on the **same computer**.

```bash
./stop.sh
```

Docker:

```bash
docker compose up -d
```

For a phone-reachable URL see CLOUD_DEPLOY.md.
